stages:
  annotate:
    cmd: python -m pet_annotation.cli annotate --annotator llm --dry-run
    deps:
      - src/pet_annotation/teacher/
      - params.yaml
    params:
      - annotation
      - models
    outs:
      - reports/annotation_stats.json:
          cache: false
